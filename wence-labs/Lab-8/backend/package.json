{
  "name": "backend",
  "version": "1.0.0",
  "description": "## 1. **Setup Environment**",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "precommit": "powershell cd .. && cd .. && cd .. && cd .. && powershell -ExecutionPolicy Bypass -File .\\scripts\\prep-commit-msg.ps1 && exit && exit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0",
    "fs": "^0.0.1-security"
  }
}
